import styled from 'styled-components';

const ImagePreview = styled.img`
  width: 100%;
  max-width: 200px;
  height: auto;
  border-radius: 8px;
  margin-top: 0.5rem;
`;

export default ImagePreview; 